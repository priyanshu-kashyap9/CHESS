# Vite + React Chess Game
1. First of all go to terminal and type `cd GAME` 
2. Its codmplet than agian type in terminal
3. type - `cd chess`
## Quick Start
1. Run `npm install`
2. Start with `npm run dev`
3. Open the local URL shown in the terminal (usually http://localhost:5173)
 
